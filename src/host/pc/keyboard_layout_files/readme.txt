This directory contains files for different keyboard layouts that can be programmed into
the Snopf device.

Each file is a json file that includes a tuple of integers (first is key modifier, second is keycode) for each of the 64 characters in the Snopf character table.
